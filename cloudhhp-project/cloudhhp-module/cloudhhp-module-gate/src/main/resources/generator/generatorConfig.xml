<!DOCTYPE generatorConfiguration
    PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
    "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <properties resource="generator/config.properties"/>

    <context id="Mysql" targetRuntime="MyBatis3Simple" defaultModelType="flat">
        <property name="beginningDelimiter" value="`"/>
        <property name="endingDelimiter" value="`"/>

        <plugin type="tk.mybatis.mapper.generator.MapperPlugin">
            <property name="mappers" value="tk.mybatis.mapper.common.Mapper"/>
            <property name="caseSensitive" value="true"/>
            <property name="lombok" value="Getter,Setter,ToString"/>
        </plugin>

        <jdbcConnection driverClass="${jdbc.driverClass}"
                        connectionURL="${jdbc.url}"
                        userId="${jdbc.user}"
                        password="${jdbc.password}">
        </jdbcConnection>

        <!--实体-->
        <javaModelGenerator targetPackage="com.hehanpeng.framework.cloudhhp.module.core.domain.entity.${moduleName}"
                            targetProject="src/main/java"/>

        <!--mapper.xml-->
        <sqlMapGenerator targetPackage="com.hehanpeng.framework.cloudhhp.module.core.dao.${moduleName}"
                         targetProject="src/main/resources"/>

        <!--mapper接口-->
        <javaClientGenerator targetPackage="com.hehanpeng.framework.cloudhhp.module.core.dao.${moduleName}"
                             targetProject="src/main/java"
                             type="XMLMAPPER"/>

        <!--为哪张表生成代码-->
        <table tableName="${tableName}">
            <generatedKey column="id" sqlStatement="JDBC"/>
        </table>
        <table tableName="${tableName1}">
            <generatedKey column="id" sqlStatement="JDBC"/>
        </table>
        <!--为哪张表生成代码-->
        <table tableName="${tableName2}">
            <generatedKey column="id" sqlStatement="JDBC"/>
        </table>
        <!--为哪张表生成代码-->
        <table tableName="${tableName3}">
            <generatedKey column="id" sqlStatement="JDBC"/>
        </table>
        <!--为哪张表生成代码-->
        <table tableName="${tableName4}">
            <generatedKey column="id" sqlStatement="JDBC"/>
        </table>
        <!--为哪张表生成代码-->
        <table tableName="${tableName5}">
            <generatedKey column="id" sqlStatement="JDBC"/>
        </table>
        <!--为哪张表生成代码-->
        <table tableName="${tableName6}">
            <generatedKey column="id" sqlStatement="JDBC"/>
        </table>
        <table tableName="${tableName7}">
            <generatedKey column="id" sqlStatement="JDBC"/>
        </table>
        <table tableName="${tableName8}">
            <generatedKey column="id" sqlStatement="JDBC"/>
        </table>
        <table tableName="${tableName9}">
            <generatedKey column="id" sqlStatement="JDBC"/>
        </table>
    </context>
</generatorConfiguration>